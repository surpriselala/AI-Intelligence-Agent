"""Reserved for AI news processing in a later version."""

from typing import Any


def summarize_news_items(news_items: list[dict[str, Any]]) -> list[dict[str, Any]]:
    """Return news items unchanged until news summarization is implemented."""
    return news_items

"""AI news collection placeholder for a later version."""

from typing import Any


def collect_ai_news(max_results: int = 10) -> list[dict[str, Any]]:
    """Return collected AI news once version 2 news support is implemented."""
    if max_results <= 0:
        return []
    return []

services:
  ai-intelligence-agent:
    build:
      context: .
    env_file:
      - .env
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./outputs:/app/outputs
    command: python main.py
